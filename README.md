> **This repository has moved and is archived.**
>
> Its contents are now [`log/`](https://github.com/metasequoiaime/MSIME-Windows/tree/main/log) in
> [metasequoiaime/MSIME-Windows](https://github.com/metasequoiaime/MSIME-Windows), which holds the shared logging library.
> Full history was preserved through the move, so `git log` and `git blame` follow files into their new paths.
>
> Open issues and pull requests belong in MSIME-Windows. Nothing here will be updated.

# MetasequoiaImeLog

Capture logs for Metasequoia IME modules.
